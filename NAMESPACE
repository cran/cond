# Export all names
exportPattern(".")

# Import all packages listed as Imports or Depends
import(
  statmod,
  survival
)

# Define global functions or variables
importFrom("graphics", "abline", "close.screen", "legend", "lines",
             "locator", "matplot", "par", "points", "screen", "segments",
             "split.screen", "text", "title")
importFrom("stats", "as.formula", "contrasts", "deviance", "dnorm",
             "glm.control", "is.empty.model", "lm.fit",
             "model.frame.default", "model.matrix", "model.response",
             "pnorm", "predict", "qchisq", "qnorm", "smooth.spline",
             "spline", "update")
importFrom("utils", "menu")

# Define S3 methods
S3method(cond, glm)
S3method(plot, cond)
S3method(print, cond)
S3method(summary, cond)
S3method(print, summaryCond)
